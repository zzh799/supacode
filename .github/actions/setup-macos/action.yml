name: Setup macOS build deps
description: Install + cache mise tools and ThirdParty build outputs (Ghostty, zmx)

inputs:
  disable-tools:
    description: Comma-separated mise tools to skip, so CI installs only what it needs.
    default: spm:swiftlang/swift-format,npm:create-dmg
  install-fish:
    description: Install Fish for shell-compatibility regression tests.
    default: "false"

runs:
  using: composite
  steps:
    # Job-wide so every later mise call (install, plus auto-install during the
    # build steps under MISE_YES) skips the unneeded tools, not just this step.
    - name: Limit mise to the tools CI needs
      shell: bash
      run: echo "MISE_DISABLE_TOOLS=${{ inputs.disable-tools }}" >> "$GITHUB_ENV"
    # The bundled zig 0.15.2 linker can't link the macOS 26.4+ SDK (ziglang/zig#31658).
    # Reuse the shared selector so CI and local agree on the Zig-linkable Xcode.
    - name: Select a Zig-linkable Xcode
      shell: bash
      run: |
        set -euo pipefail
        developer_dir="$(./scripts/select-developer-dir.sh)"
        sudo xcode-select -s "$developer_dir"
        xcodebuild -version
    - uses: jdx/mise-action@v4
      with:
        version: 2026.3.0
        cache: true
    - name: Install Fish
      if: ${{ inputs.install-fish == 'true' }}
      shell: bash
      run: brew list fish >/dev/null 2>&1 || brew install fish
    - name: Authenticate with Tuist
      if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
      shell: bash
      run: mise exec -- tuist auth login
    - name: Xcode cache key
      shell: bash
      run: |
        XCODE_VERSION="$(xcodebuild -version | tr '\n' '-' | sed 's/[^A-Za-z0-9._-]/_/g; s/-$//')"
        printf '%s\n' "XCODE_VERSION=$XCODE_VERSION" >> "$GITHUB_ENV"
    - name: ThirdParty cache keys
      shell: bash
      run: |
        set -euo pipefail
        printf '%s\n' "GHOSTTY_SHA=$(git -C ThirdParty/ghostty rev-parse HEAD)" >> "$GITHUB_ENV"
        printf '%s\n' "ZMX_SHA=$(git -C ThirdParty/zmx rev-parse HEAD)" >> "$GITHUB_ENV"
    # Key on only the inputs that change the built artifact (submodule SHA, build
    # script, zig version, patches) so unrelated action/project edits don't evict it.
    - name: Ghostty cache
      id: ghostty_cache
      uses: actions/cache@v5
      with:
        path: .build/ghostty
        key: ${{ runner.os }}-${{ runner.arch }}-xcode-${{ env.XCODE_VERSION }}-ghostty-v3-${{ env.GHOSTTY_SHA }}-${{ hashFiles('scripts/build-ghostty.sh', 'mise.toml', '.gitmodules', 'patches/ghostty/*.patch') }}
    - name: Build ghostty
      if: steps.ghostty_cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        set -euo pipefail
        make build-ghostty-xcframework
    - name: zmx cache
      id: zmx_cache
      uses: actions/cache@v5
      with:
        path: .build/zmx
        key: ${{ runner.os }}-${{ runner.arch }}-xcode-${{ env.XCODE_VERSION }}-zmx-v1-${{ env.ZMX_SHA }}-${{ hashFiles('scripts/build-zmx.sh', 'mise.toml', '.gitmodules', 'patches/zmx/*.patch') }}
    - name: Build zmx
      if: steps.zmx_cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        set -euo pipefail
        make build-zmx
